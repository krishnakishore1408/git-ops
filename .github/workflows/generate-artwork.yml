name: Artwork Workflow

on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Install Cowsay
          uses: sudo apt install cowsay -y

        - name: Generate ASCII Artwork
          run: cowsay -f dragon "Run for me ..." >>dragon.txt 