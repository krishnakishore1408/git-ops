name: Sample Workflow

on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome to my workflow
          run: echo "Welcome"

        -name: List file
         run: ls

         -name: Readfile
          run: cat README.md

